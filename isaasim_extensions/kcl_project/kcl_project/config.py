# kcl_project/config.py

# Docking marker configuration constants
DOCKING_MARKER_CONFIG = {
    "dragon_marker": {
        "position": (-15, 171.4, 185.3),  # Relative to DragonX origin
        "orientation": (1.0, 0.0, 0.0, 0.0)  # No rotation (neutral orientation) (w, x, y, z)
    },
    "iss_marker": {
        "position": (389.2, -40.3, -94.5),  # Relative to ISS origin
        "orientation": (1.0, 0.0, 0.0, 0.0)  # No rotation (neutral orientation) (w, x, y, z)
    }
}
